#include <stdio.h>
#include <time.h>

void print_corerain();
void print_CRT();
void print_program_info(const char *prog_name);
void printf_cr(int line, const char *func, const char *file, const char *format, ...);
